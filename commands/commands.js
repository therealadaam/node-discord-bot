module.exports = {
	name: 'commands',
	description: 'Gets all commands this bot currently accepts and info on them',
	execute(message) {
		message.reply('Not much... This one, joke, and prune');
	},
};
